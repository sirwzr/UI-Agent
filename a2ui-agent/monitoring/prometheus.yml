# ===== Prometheus 配置 =====
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "a2ui-agent"
    metrics_path: "/api/metrics"
    static_configs:
      - targets: ["app:3000"]
    scrape_interval: 10s
