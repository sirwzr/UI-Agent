# A2UI Agent — 新电脑环境配置指南

## 前提条件

确保新电脑已安装以下软件：

| 依赖 | 最低版本 | 验证命令 |
|------|---------|---------|
| Node.js | ≥ 22 | `node -v` |
| pnpm | 最新 | `npm i -g pnpm` |
| PostgreSQL | ≥ 14 | `psql --version` |
| Redis | ≥ 6 | `redis-cli ping` |

> 如果有 Docker，可用 `docker-compose up -d` 一键启动 PostgreSQL + Redis，无需单独安装。

## 1. 克隆项目

```bash
git clone https://github.com/sirwzr/UI-Agent.git
cd UI-Agent/a2ui-agent
```

## 2. 安装依赖

```bash
pnpm install
```

## 3. 配置环境变量

```bash
cp .env.example .env
```

然后编辑 `.env` 文件，填入各 API Key（参见 `API密钥配置清单.md`）。

## 4. 初始化数据库

```bash
pnpm db:generate
pnpm db:push
```

## 5. 启动开发服务器

```bash
pnpm dev
```

浏览器访问 `http://localhost:3000`

## 6. 可选：Docker 启动数据库

如果不想本地安装 PostgreSQL 和 Redis：

```bash
docker-compose up -d
```

然后执行步骤 4-5。

## 常用命令

| 命令 | 用途 |
|------|------|
| `pnpm dev` | 启动开发服务器 (端口 3000) |
| `pnpm build` | 生产构建 |
| `pnpm test` | 运行单元测试 |
| `pnpm test:e2e` | 运行 E2E 测试 |
| `pnpm lint` | 代码检查 |
| `pnpm db:studio` | 打开 Prisma 数据库管理界面 |
